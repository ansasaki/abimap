# Testing add strategy
-
  input:
    args:
      - "update"
      - "--add"
      - "--out"
      - "tc1.map"
      - "tc1.map"
    stdin: "tc1.in"
  output:
    file: "tc1.out"
    stdout: "tc1.stdout"
    warnings:
      - "Overwriting existing file 'tc1.map'"
      - "Given paths in '--out' and 'file' are the same."
      - "Moving 'tc1.map' to 'tc1.map.old'."
    exceptions:
