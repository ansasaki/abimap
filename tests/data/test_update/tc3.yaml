# Testcases to test new command
-
  input:
    args:
      - "update"
      - "--symbols"
      - "--out"
      - "tc3.map"
      - "tc3.map"
    stdin: "tc3.in"
  output:
    file: "tc3.out"
    stdout: "tc3.stdout"
    warnings:
      - "Overwriting existing file 'tc3.map'"
      - "Given paths in '--out' and 'file' are the same."
      - "Moving 'tc3.map' to 'tc3.map.old'."
    exceptions:
